# Runtime Output

This directory stores runtime logs.

- `crawler.log`: logs emitted by the Python crawler through `tools.utils.logger`.
- `webui.log`: logs shown in the WebUI system console, including scheduler and monitor events.

Log files are ignored by git. The directory itself is kept in the repository.
